name: Run local script on pi

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Run local script
        run: /home/peter/websrv/scripts/gha_push_main.sh
